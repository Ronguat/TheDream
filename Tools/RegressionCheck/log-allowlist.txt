# Engine-side warnings the universal set tolerates. One line per allowed substring, with the reason
# directly above it. Seeded from the first run of the new loop, 2026-09-03.
#
# An entry here is not a dismissal. It is a claim that the line is understood, and the reason has to
# say what it is. Adding one is a visible edit, the same shape as Tools/CommentCheck/baseline.txt.

# OPEN FINDING, not benign, and deliberately not fixed by the audit that found it.
# Three ReleaseStartSeconds values are stale against their Release Window notifies:
#   swing 1      notify 0.7245, stored 0.6688  (55.7 ms)
#   swing 2      notify 0.5206, stored 0.4806  (40.0 ms)
#   get-up attack  notify 0.6685, stored 0.6333  (35.2 ms)
# This is the filed notify/EntrySeconds trap firing for real: the value is stored rather than
# computed, so authoring a notify desynchronises it silently, and the cost is a play-rate distortion
# rather than an error. The game's own ungated warning has been saying so; nothing read it, because
# the checker's slice keeps only the combat trace and drops every engine line.
# Allowlisted so the rest of the matrix stays readable while the designer rules. Re-deriving means
# re-running Tools/AnimPipeline/ue_fit_tier_montages.py and Tools/ValuesSnapshot/ue_seed_cells.py
# together -- a change to how the game plays, and so not the audit's to make.
ReleaseStartSeconds is
